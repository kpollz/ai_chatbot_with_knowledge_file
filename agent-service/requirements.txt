# Pinned to the set verified working in this container. CopilotKit's official
# example pins the same way, and warns that copilotkit can otherwise pull in a
# different ag-ui-langgraph than the one the endpoint helper expects.
fastapi==0.140.0
uvicorn[standard]==0.51.0
python-dotenv==1.2.2
pydantic==2.13.4
httpx==0.28.1

copilotkit==0.1.94
ag-ui-langgraph[fastapi]==0.0.42
ag-ui-protocol==0.1.19

langchain==1.3.14
langchain-openai==1.4.1
langgraph==1.2.9

# Threads live in Postgres, not in the process.
langgraph-checkpoint-postgres==3.1.0
psycopg[binary,pool]==3.3.4

langfuse==4.14.1
