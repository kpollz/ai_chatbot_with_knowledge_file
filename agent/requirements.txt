# Pinned to the set verified working. CopilotKit's official example pins the same
# way, and warns that copilotkit can otherwise pull in a different
# ag-ui-langgraph than the endpoint helper expects.

# --- agent ---
langgraph==1.2.9
# Meta package — required by langfuse.langchain CallbackHandler
langchain==1.3.14
langchain-openai==1.4.1
langchain-core
httpx==0.28.1
python-dotenv==1.2.2
pydantic==2.13.4
langfuse==4.14.1

# --- AG-UI server ---
fastapi==0.140.0
uvicorn[standard]==0.51.0
copilotkit==0.1.94
ag-ui-langgraph[fastapi]==0.0.42
ag-ui-protocol==0.1.19

# Threads live in Postgres, not in the process.
langgraph-checkpoint-postgres==3.1.0
psycopg[binary,pool]==3.3.4
